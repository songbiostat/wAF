# setwd("E:/OSU/Research/AF SNV/R Package/wAF")
# library(devtools)
#
# # RV_dense
# set.seed(7)
# RV_dense <- data_sim(n = 1000, K = 300, prop = 0.2, strg = 0.5, binary = TRUE)
# use_data(RV_dense)
#
# # RV_sparse
# set.seed(14)
# RV_sparse <- data_sim(n = 1000, K = 300, prop = 0.01, strg = 2, binary = TRUE)
# use_data(RV_sparse)
#
# # SNV_dense
# set.seed(21)
# SNV_dense <- data_sim(n = 1000, K = 100, prop = 0.2, strg = 0.2)
# use_data(SNV_dense)
#
# # SNV_sparse
# set.seed(28)
# SNV_sparse <- data_sim(n = 1000, K = 100, prop = 0.01, strg = 0.5)
# use_data(SNV_sparse)
